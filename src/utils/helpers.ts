// export function safeParse(jsonString: string | null, fallback = null) {
//     try {
//       return jsonString ? JSON.parse(jsonString) : fallback;
//     } catch (error) {
//       console.error('Error parsing JSON:', jsonString);
//       return fallback;
//     }
//   }